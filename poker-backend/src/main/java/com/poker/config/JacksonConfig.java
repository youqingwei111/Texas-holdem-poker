package com.poker.config;

import org.springframework.context.annotation.Configuration;

/**
 * Jackson配置
 */
@Configuration
public class JacksonConfig {
    // Spring Boot默认配置已足够，如需自定义可在此配置ObjectMapper
}